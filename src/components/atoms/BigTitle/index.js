import React from 'react';
import {Text} from 'react-native';

export default function index({title}) {
  return (
    <Text style={{fontSize:33, color:'#33346D', fontWeight:'bold'}}>{title}</Text>
  )
}
